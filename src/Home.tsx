 const Home = () => {
    return  (
     <div>
         <h1>Aqui é tela home </h1>
         <button onClick={() => null} >DARK MODE</button>
        <button onClick={() => null} >LIGHT MODE</button>  
     </div>
    );
} 

export default Home;